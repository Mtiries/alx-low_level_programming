#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char letter;

	for (;letter = 'a'; c <= 'z'; r++)
		putchar(c);

	putchar('\n');

	return (0);
}


















